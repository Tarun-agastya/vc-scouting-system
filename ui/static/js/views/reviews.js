/* SCOUT — Review Inbox view (stub — implemented in a later UI phase). */
export default {
  title: "Review Inbox",
  mount(el) {
    el.innerHTML = `<div class="empty">
        <div class="empty__title">Review Inbox</div>
        <div>This page is built in a later phase.</div>
      </div>`;
  },
};
