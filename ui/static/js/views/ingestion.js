/* SCOUT — Ingestion Control view (stub — implemented in a later UI phase). */
export default {
  title: "Ingestion Control",
  mount(el) {
    el.innerHTML = `<div class="empty">
        <div class="empty__title">Ingestion Control</div>
        <div>This page is built in a later phase.</div>
      </div>`;
  },
};
