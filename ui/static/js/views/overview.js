/* SCOUT — Overview view (stub — implemented in a later UI phase). */
export default {
  title: "Overview",
  mount(el) {
    el.innerHTML = `<div class="empty">
        <div class="empty__title">Overview</div>
        <div>This page is built in a later phase.</div>
      </div>`;
  },
};
