/* SCOUT — Browse & Search view (stub — implemented in a later UI phase). */
export default {
  title: "Browse & Search",
  mount(el) {
    el.innerHTML = `<div class="empty">
        <div class="empty__title">Browse & Search</div>
        <div>This page is built in a later phase.</div>
      </div>`;
  },
};
