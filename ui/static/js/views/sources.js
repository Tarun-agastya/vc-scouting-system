/* SCOUT — Sources view (stub — implemented in a later UI phase). */
export default {
  title: "Sources",
  mount(el) {
    el.innerHTML = `<div class="empty">
        <div class="empty__title">Sources</div>
        <div>This page is built in a later phase.</div>
      </div>`;
  },
};
